# Server
server.port=8080

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/mdm_db
spring.datasource.username=mdm_user
spring.datasource.password=mdm_password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.com.mdm=DEBUG
logging.level.org.springframework.security=INFO
```

- Press **Ctrl + S** to save

---

### 3. Run the project
- On the left panel find:
```
src/main/java/com/mdm/mdmbackend/MdmBackendApplication.java
```
- Right click on it ? **"Run MdmBackendApplication"**
- Watch the console at the bottom

---

## ? How to Test

In the console at the bottom you should see something like:
```
Started MdmBackendApplication in 3.5 seconds
Tomcat started on port(s): 8080
```

**No red errors** related to database connection ?

---

### ?? If you see this error:
```
Connection refused or password authentication failed